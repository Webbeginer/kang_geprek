import CartClient from "@/components/CartClient";

export default function CartPage() {
  return (
    <div className="h-[110vh] flex justify-center items-center bg-slate-100">
      <CartClient />
    </div>
  );
}
